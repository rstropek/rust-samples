[package]
name = "fractal_tree_cli"
version = "0.1.0"
edition = "2021"

[dependencies]
fractal_tree = { path = "../fractal_tree", default-features = false, features = ["svg"] }
tokio = { version = "1", features = ["fs", "macros", "rt", "io-util"] }
